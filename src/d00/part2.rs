use super::help;

pub fn run() -> u128 {
    0
}

#[cfg(test)]
mod tests {
    // use insta::assert_debug_snapshot;

    use super::*;

    #[test]
    fn test_run() {
        assert_eq!(run(), 123);
    }
}
