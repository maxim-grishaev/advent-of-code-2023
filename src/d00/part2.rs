use super::help;

pub fn run() -> u128 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;
    // use insta::assert_debug_snapshot;

    #[ignore = "todo"]
    #[test]
    fn test_run() {
        assert_eq!(run(), 123);
    }
}
